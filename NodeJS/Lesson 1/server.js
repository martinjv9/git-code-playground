const { add, subtract, multiply, divide } = require("./math");
const os = require("os");

console.log(add(1, 2));

//console.log(global);

// console.log(os.type());
// console.log(os.version());
// console.log(os.homedir());
